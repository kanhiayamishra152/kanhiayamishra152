name: unit_converter
description: A premium unit converter application with neumorphic design.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.1
  
  # UI & Design
  google_fonts: ^6.1.0
  flutter_neumorphic: ^3.2.0
  glassmorphism: ^3.0.0
  
  # Icons
  font_awesome_flutter: ^10.6.0
  
  # Utilities
  intl: ^0.18.1
  clipboard: ^0.1.3
  
  # Haptic Feedback
  vibration: ^1.8.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
